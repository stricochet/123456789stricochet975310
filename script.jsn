// Fonction pour la recherche (simple exemple)
function searchFilm() {
  const query = document.getElementById('search-input').value;
  if (query) {
    alert(`Recherche pour : ${query}`);
    // Ici tu peux intégrer une fonction de recherche sur une API de films ou quelque chose d'autre.
  }
}
